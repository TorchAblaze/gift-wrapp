<h1>Gifts</h1>

<%= link_to "Add gift", gifts_add_path %>

<% @gifts.each do |gift| %>
  <div>
    <p>
    Gift idea: 
      <span><%= gift.name %></span>
    For:
      <span><%= gift.person.name %></span>
    </p>
  </div>  
<% end %>
